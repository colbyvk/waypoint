# Sound call-graph extraction for TS / JS / TSX / JSX / Rust (the coverage frontier).
# Kept SEPARATE from requirements.txt on purpose: it is installed best-effort by
# detectors/install.sh so a missing wheel on some platform degrades the clike graph
# to the regex extractor (graceful) instead of breaking the whole install. Python
# always uses stdlib `ast` (sound, zero-dep) — these are only for the other langs.
tree-sitter==0.25.2
tree-sitter-typescript==0.23.2
tree-sitter-javascript==0.25.0
tree-sitter-rust==0.24.2
