============================= test session starts ==============================
collected 1 item

test_property_samples.py F                                                [100%]

=================================== FAILURES ===================================
_________________________________ test_clamp __________________________________

    @given(x=st.integers(), lo=st.integers(), hi=st.integers())
>   def test_clamp(x, lo, hi):
        assume(lo <= hi)
        result = clamp(x, lo, hi)

money/clamp.py:14: in clamp
    return min(lo, max(x, hi))
E   assert -1 <= 0
E    +  where -1 = clamp(0, 0, -1)

----------------------------- Captured stdout call -----------------------------
Falsifying example: test_clamp(
    x=0, lo=0, hi=-1,
)
=========================== short test summary info ============================
FAILED test_property_samples.py::test_clamp - assert -1 <= 0
============================== 1 failed in 0.21s ===============================
